@page "/PrintOrder/{OrderId:int}"

@rendermode @(new InteractiveServerRenderMode(prerender: false))

@if (OrderId > 0)
{
    <DEALER.Web.Components.Pages.Sales.OrdersSells.ComOrderDetails OrderId="@OrderId"></DEALER.Web.Components.Pages.Sales.OrdersSells.ComOrderDetails>
}

@code {
    [Parameter]
    public int OrderId { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
    }
}